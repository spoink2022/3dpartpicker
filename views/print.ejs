<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Add Map</title>

    <link rel="stylesheet" type="text/css" href="../css/print.css" />
    <script src="../js/print.js"></script>
  </head>
  <body>
    <script>
        let part = <%- JSON.stringify(part) %>;
        let user = <%- JSON.stringify(user) %>;
        let avatarUrl = '<%- avatarUrl %>';
        let sellers = <%- JSON.stringify(part) %>;
    </script>
    <h3>My Google Maps Demo</h3>
    <!--The div element for the map -->
    <div id="map"></div>
    <form id="checkoutForm">
      <input id="" name="">
    </form>

    <form action="/findSeller" action="post" id="coordinateForm">
      <input id="latCoord" name="lat" type="hidden">
      <input id="lonCoord" name="lon" type="hidden">
      <button id="coordinateButton" type="submit"></button>
    </form>

    <!-- Async script executes immediately and must be after any DOM elements used in callback. -->
    <script
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCkJ_T9KxMbO4VFLl5kXveos7ZvYqrTbEs&callback=initMap&libraries=&v=weekly"
      async
    ></script>
  </body>
</html>